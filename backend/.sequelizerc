const { resolve } = require("path");

module.exports = {
  "config": resolve(__dirname, "whatstalk", "config", "database.js"),
  "modules-path": resolve(__dirname, "whatstalk", "models"),
  "migrations-path": resolve(__dirname, "whatstalk", "database", "migrations"),
  "seeders-path": resolve(__dirname, "whatstalk", "database", "seeds")
};
